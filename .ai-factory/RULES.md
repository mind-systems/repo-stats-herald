# Project Base Rules

> Counter-defaults the executor would otherwise violate by its own defaults, each with its why. Generic style/formatting conventions — case styles, formatting, boilerplate error/logging idioms — are deliberately excluded. This project surfaced no counter-default, so the file stays empty below this note; that is the correct result, not a gap to fill.
