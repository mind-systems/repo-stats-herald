## Code Review Summary

**Artifact:** Plan `47-10-2-report-schedules-delivery.md` (task 10.2 — Report schedules + delivery), round 3
**Files Reviewed:** plan + spec `18-weekly-schedule-delivery.md` + rounds 1–2 reviews + ground-truth code (`changelog/report.py`, `changelog/sections/{__init__,summary,per_branch,remaining}.py`, `commits/collector.py`, `github/mirror.py`, `ingestion/served_repos.py`, `core/config.py`, `routing/resolver.py`, `delivery/{service,telegram}.py`, `knowledge/sync.py`, `graph/coordination.py`, `episodic/backfill.py`, `scripts/{backfill,eval}.py`, `src/main.py`, `Makefile`, `.ai-factory/ROADMAP.md`)
**Files Reviewed:** count 20
**Risk Level:** 🟢 Low — rounds 1 and 2's critical findings are both resolved and re-verified against the code; no new blocking issue found.

### Context Gates
- **Roadmap** — OK. `# Plan: 10.2 — Report schedules + delivery` matches `.ai-factory/ROADMAP.md:101` verbatim; every plan guard (iterate `served_repos` not mirror-root; ensure-before-build; `None`→nothing; consume only `telegram_channel`/`language`, never `branch_role`/`is_release`/`is_prerelease`; idempotent re-run) traces to the contract line and spec `.ai-factory/specs/18-weekly-schedule-delivery.md`.
- **Architecture** — OK. `scripts/report.py` is a composition root (concretes wired only there, mirroring `backfill.py`/`eval.py`); feature classes keep receiving abstractions via constructors; `resolve_canonical_ref` lands in infra (`src/github/mirror.py`, co-located with `RepoMirror.default_branch`). No boundary/dependency violation. `TimeWindow`'s new `RepoMirror`/`GitCommitCollector` imports into `src/changelog/` introduce no import cycle (`mirror.py` imports only `github/app_auth`; `collector.py` imports only `commits/models`).
- **Rules** — `.ai-factory/RULES.md` is intentionally empty. No skill-context file at `.ai-factory/skill-context/aif-review/SKILL.md`. No violations.

### Round-1 & round-2 findings — all resolved
- **R1-#1 / R2-#1 (EMPTY_TREE crash in `PerBranchSection`)** — the whole crash surface is now closed on both boundaries. Task 2 makes `active_branches` empty-tree-safe: `after == EMPTY_TREE_SHA` returns `[]` immediately (covers the empty-repo `(EMPTY_TREE_SHA, EMPTY_TREE_SHA)` range — round-2's gap), and `before == EMPTY_TREE_SHA` with a real `after` skips `commit_timestamp(before)`, sets `before_time = None`, drops `--since`, and sets `branch_before = EMPTY_TREE_SHA`. Verified against `collector.py:148-149` (both `commit_timestamp` calls run before the branch loop, with `check=True` + `datetime.fromisoformat`, so both are the real crash sites) and `per_branch.py:41`. Task 7 now mandates the section-interaction crash-guard test for **both** cases (young-repo `before = EMPTY_TREE_SHA` real `after`; empty-repo `before = after = EMPTY_TREE_SHA`).
- **R1-#2 (partial "one home per fact")** — Task 3 migrates **all three** copies onto `resolve_canonical_ref`. Verified byte-identical: `sync.py:35`, `coordination.py:37`, `backfill.py:62` each are `override = self._canonical_refs.get(repo); if override is not None: return override; return self._mirror.default_branch(repo)`.
- **R1-#3 (no per-repo error isolation)** — Task 5 wraps each served repo's `ensure → resolve-plan → build → deliver` in `try/except Exception` with logged failure + `continue` + run-level summary count.
- **Deferred (`Makefile .PHONY`, missing `deploy/`)** — Task 6 now adds `report-daily`/`report-weekly` to `.PHONY` (confirmed `Makefile:4` = `.PHONY: install run tunnel dev eval test`) and notes `deploy/` is created by the write (confirmed absent today).

### Critical Issues
None.

### Positive Notes
- **Signatures verified end-to-end against the plan's call sites:** `default_section_registry(mirror, resolver, reasoner, collector, source_strategy, llm, remaining_prompt)` (`sections/__init__.py:14`); `Report.build(repo, org_id, lang="ru")` resolving the window once and rendering every section with the same `(before, after)` (`report.py:53-64`); `report_for_schedule(schedule, registry)` is 2-arg today (`report.py:76`), so the keyword-only `mirror/collector/canonical_refs=None` extension keeps `test_schedule.py`'s 2-arg call green; `TimeWindow` is a `frozen` dataclass with `delta` as its sole field (`report.py:25-35`), so `field(default=None, compare=False)` additions preserve delta-only equality and `TimeWindow(timedelta(days=1))` construction (non-default `delta` precedes the defaulted fields — valid ordering); `DeliveryPlanResolver.resolve(org_id, repo, branch)` (`resolver.py:20`), `DeliveryService.deliver(plan, note)` (`service.py:18`), `TelegramClient(token)` (`telegram.py:6`), `RepoMirror.ensure/sweep_worktrees/object_store_path/default_branch` — all match.
- **`DeliveryPlan` guard is sound:** `resolver.py:22-28` computes `branch_role`/`is_release`/`is_prerelease` from the canonical ref but the report path reads only `telegram_channel`/`language`; `DeliveryService.deliver` (`service.py:20-24`) logs-and-skips when `telegram_channel is None`, exactly the spec's unmapped-org guard.
- **Empty-tree safety of the sibling sections is real, not assumed:** `SummarySection` (`summary.py:31-34`) returns `None` on `not change.commits.commits` (a `LinkedChangeResolver.resolve(before..after)` over an empty range), and `RemainingSection` (`remaining.py:57-65`) reads the roadmap blob at `after` and returns `None` when `read_blob` yields `None` (empty-tree `after` → `git show` non-zero → `None`) — so only `PerBranchSection` needed the Task 2 fix.
- **Schema-execution list is exact:** Task 5's `ingestion` + `knowledge` + `episodic` + `graph` matches `main.py:28-31,41-44`; all four `schema.sql` files exist. `ingestion/schema.sql` is what makes `served.all()` work on a fresh DB.
- **Wiring fidelity:** the Reasoner assembly ("as `scripts/eval.py` wires it") maps to `eval.py:216-224`; the `clone_source` closure + `RepoMirror` + `sweep_worktrees()`-before-`ensure` pattern matches `backfill.py:54-60` and `main.py:71-78`; `create_pool(settings.postgres_dsn)` from `src.core.db` and the `try/finally: await pool.close()` shape match both reference roots.
- **`commit_at_or_before` reuses the proven idiom:** `_rev_list(repo_path, "-1", f"--until={when.isoformat()}", ref=ref)` already appends `--end-of-options` before the ref (`collector.py:187`) and returns `[]` on non-zero (`check=False`), so `results[0] if results else None` is a faithful, read-only, no-raise primitive — the same `-1 --until=` shape `active_branches` uses at `collector.py:160`.
- **`TimeWindow.resolve` behavior model is internally consistent:** a quiet window (canonical tip older than `now - delta`) yields `before == after == tip` (both `commit_at_or_before` calls return the tip) → empty range → every section `None` → `build` `None`; an active window yields the in-window commits; a whole-history-younger-than-`delta` repo yields `before = EMPTY_TREE_SHA` with a real `after`, which Task 2's fix renders without raising; an empty repo yields `(EMPTY_TREE_SHA, EMPTY_TREE_SHA)` → `[]` from `active_branches` → `None` → logged skipped-empty (not failed).
- **Reused single `Report` across repos is correct:** the window carries repo-agnostic collaborators and `resolve(repo)`/`build(repo, org_id)` take the repo per-iteration, so one `report_for_schedule(...)` object serves every served repo in the loop.

## Deferred observations
- Affects: task 10.3 (report localization) — Task 5 delivers at `plan.language` (default `"ru"`); the plan notes 10.3 threads non-RU via the localizer. Single-language build is the correct 10.2 scope; noted only so the RU-only delivery isn't mistaken for an omission.

PLAN_REVIEW_PASS
