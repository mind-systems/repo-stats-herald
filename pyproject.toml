[project]
name = "repo-stats-herald"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "httpx",
    "pydantic-settings>=2.14.2",
    "pyyaml",
    "uvicorn[standard]",
]
